{
  "name": "%%PROJECT_NAME_PREACT%%",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build && vite build --mode client",
    "dev": "vite",
    "deploy": "$npm_execpath build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "@sonikjs/preact": "^0.0.9",
    "hono": "^3.7.6",
    "preact": "^10.17.1",
    "preact-render-to-string": "^6.2.1",
    "sonik": "^0.0.80"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "@hono/vite-dev-server": "^0.0.7",
    "@sonikjs/cloudflare-pages": "^0.0.1",
    "vite": "^4.4.11",
    "wrangler": "^3.13.1"
  }
}