{
  "name": "%%PROJECT_NAME_BASIC%%",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "deploy": "$npm_execpath build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "hono": "^3.7.6",
    "sonik": "^0.0.80"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230518.0",
    "@sonikjs/cloudflare-pages": "^0.0.1",
    "vite": "^4.4.11",
    "wrangler": "^3.13.1"
  }
}